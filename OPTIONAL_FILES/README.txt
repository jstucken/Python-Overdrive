The files in this directory are the only files which need to be moved
to other parts of the filesystem (optional). Generally, for your convenience, all
the working files are stored within the DET-Python-Anki-Overdrive directory.


Please make a backup first of these files before overwriting them at the following locations:

/etc/bash.bashrc
/etc/sudoers